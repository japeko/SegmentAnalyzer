package com.segmentanalyzer.domain.model

/** The time window the Rides page's quick-stats summary is rolled up over. */
enum class SummaryPeriod {
    THIS_WEEK,
    THIS_MONTH,
    THIS_YEAR,
    ALL_TIME,
}
