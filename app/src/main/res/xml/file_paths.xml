<?xml version="1.0" encoding="utf-8"?>
<!-- Exposes only the cacheDir/exports subfolder FitExportRepositoryImpl writes .fit files into — see EXPORTS_SUBDIR there. -->
<paths>
    <cache-path name="exports" path="exports/" />
</paths>
