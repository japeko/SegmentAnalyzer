<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SegmentAnalyzerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SegmentAnalyzer.Splash">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.SegmentAnalyzer.Splash"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Strava OAuth redirect. Must match StravaAccountRepositoryImpl.REDIRECT_URI and
                 the check in SegmentAnalyzerApp.kt; singleTask above ensures the redirect lands
                 back in the existing app instance via onNewIntent instead of spawning a new one. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="segmentanalyzer"
                    android:host="localhost"
                    android:path="/strava-callback" />
            </intent-filter>
        </activity>

    </application>

</manifest>
